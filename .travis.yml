language: python
python:
  - "3.7"
  - "3.8"
install:
  - pip install .
  - pip install -r test-requirements.txt --upgrade
  - pip install coveralls
script:
  - pytest --cov=polybot
after_success:
  - coveralls
